[build]
builder = "NIXPACKS"
buildCommand = "pnpm install && pnpm build && cp server/simulation.py dist/simulation.py"

[deploy]
startCommand = "pnpm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
NODE_ENV = "production"
