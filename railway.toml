[build]
builder = "NIXPACKS"
buildCommand = "npm install -g pnpm@10.4.1 && npx pnpm install && npx pnpm build && cp server/simulation.py dist/simulation.py"

[deploy]
startCommand = "node dist/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
NODE_ENV = "production"
